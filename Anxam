-- ANXAM HUB v6.1 | Fixed Target System
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- تنظيف الواجهات القديمة
if PlayerGui:FindFirstChild("AnxamCustomUI") then
    PlayerGui.AnxamCustomUI:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AnxamCustomUI"
ScreenGui.Parent = PlayerGui
ScreenGui.ResetOnSpawn = false

-- الألوان الرئيسية
local BG_COLOR = Color3.fromRGB(15, 15, 22)
local CARD_COLOR = Color3.fromRGB(24, 25, 36)
local ACCENT_BLUE = Color3.fromRGB(0, 170, 255)
local TEXT_COLOR = Color3.fromRGB(240, 240, 245)

-- الإطار الرئيسي
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 560, 0, 340)
MainFrame.Position = UDim2.new(0.5, -280, 0.5, -170)
MainFrame.BackgroundColor3 = BG_COLOR
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = true
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = ACCENT_BLUE
MainStroke.Thickness = 1.5
MainStroke.Transparency = 0.4
MainStroke.Parent = MainFrame

-- الشريط العلوي
local TopBar = Instance.new("Frame")
TopBar.Size = UDim2.new(1, 0, 0, 42)
TopBar.BackgroundColor3 = Color3.fromRGB(20, 21, 30)
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -90, 1, 0)
Title.Position = UDim2.new(0, 15, 0, 0)
Title.Text = "⚡ ANXAM HUB v6.1 | Target System"
Title.TextColor3 = TEXT_COLOR
Title.TextSize = 15
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.BackgroundTransparency = 1
Title.Parent = TopBar

-- زر الإغلاق X
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 28, 0, 28)
CloseBtn.Position = UDim2.new(1, -34, 0, 7)
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(255, 90, 90)
CloseBtn.TextSize = 14
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.BackgroundColor3 = Color3.fromRGB(35, 30, 40)
CloseBtn.Parent = TopBar

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 8)
CloseCorner.Parent = CloseBtn

-- زر التصغير -
local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Size = UDim2.new(0, 28, 0, 28)
MinimizeBtn.Position = UDim2.new(1, -68, 0, 7)
MinimizeBtn.Text = "─"
MinimizeBtn.TextColor3 = TEXT_COLOR
MinimizeBtn.TextSize = 12
MinimizeBtn.Font = Enum.Font.GothamBold
MinimizeBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
MinimizeBtn.Parent = TopBar

local MiniBtnCorner = Instance.new("UICorner")
MiniBtnCorner.CornerRadius = UDim.new(0, 8)
MiniBtnCorner.Parent = MinimizeBtn

-- الزر الصغير العائم
local MiniToggle = Instance.new("TextButton")
MiniToggle.Name = "MiniToggle"
MiniToggle.Size = UDim2.new(0, 130, 0, 38)
MiniToggle.Position = UDim2.new(0.02, 0, 0.15, 0)
MiniToggle.Text = "⚡ ANXAM HUB"
MiniToggle.TextColor3 = ACCENT_BLUE
MiniToggle.TextSize = 13
MiniToggle.Font = Enum.Font.GothamBold
MiniToggle.BackgroundColor3 = BG_COLOR
MiniToggle.Active = true
MiniToggle.Draggable = true
MiniToggle.Visible = false
MiniToggle.Parent = ScreenGui

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(0, 10)
ToggleCorner.Parent = MiniToggle

MiniToggle.MouseButton1Click:Connect(function()
    MiniToggle.Visible = false
    MainFrame.Visible = true
end)

MinimizeBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    MiniToggle.Visible = true
end)

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- شريط التبويبات
local TabBar = Instance.new("Frame")
TabBar.Size = UDim2.new(0, 135, 1, -52)
TabBar.Position = UDim2.new(0, 8, 0, 46)
TabBar.BackgroundColor3 = Color3.fromRGB(18, 19, 26)
TabBar.Parent = MainFrame

local TabCorner = Instance.new("UICorner")
TabCorner.CornerRadius = UDim.new(0, 8)
TabCorner.Parent = TabBar

local TabLayout = Instance.new("UIListLayout")
TabLayout.SortOrder = Enum.SortOrder.LayoutOrder
TabLayout.Padding = UDim.new(0, 6)
TabLayout.Parent = TabBar

local Container = Instance.new("Frame")
Container.Size = UDim2.new(1, -157, 1, -52)
Container.Position = UDim2.new(0, 149, 0, 46)
Container.BackgroundColor3 = Color3.fromRGB(18, 19, 26)
Container.Parent = MainFrame

local ContainerCorner = Instance.new("UICorner")
ContainerCorner.CornerRadius = UDim.new(0, 8)
ContainerCorner.Parent = Container

local Tabs = {"اللاعب", "الأنيميشنات", "استهداف", "المواقع"}
local Pages = {}
local TabButtons = {}

for i, tabName in ipairs(Tabs) do
    local Page = Instance.new("Frame")
    Page.Name = tabName .. "_Page"
    Page.Size = UDim2.new(1, -12, 1, -12)
    Page.Position = UDim2.new(0, 6, 0, 6)
    Page.BackgroundTransparency = 1
    Page.Visible = (i == 3) -- فتح تبويب استهداف تلقائياً
    Page.Parent = Container
    Pages[tabName] = Page

    local TabBtn = Instance.new("TextButton")
    TabBtn.Size = UDim2.new(1, 0, 0, 36)
    TabBtn.Text = tabName
    TabBtn.TextColor3 = (i == 3) and ACCENT_BLUE or Color3.fromRGB(160, 160, 175)
    TabBtn.TextSize = 13
    TabBtn.Font = Enum.Font.GothamBold
    TabBtn.BackgroundColor3 = (i == 3) and CARD_COLOR or Color3.fromRGB(22, 23, 32)
    TabBtn.Parent = TabBar

    local BtnCorner = Instance.new("UICorner")
    BtnCorner.CornerRadius = UDim.new(0, 6)
    BtnCorner.Parent = TabBtn

    TabButtons[tabName] = TabBtn

    TabBtn.MouseButton1Click:Connect(function()
        for name, p in pairs(Pages) do
            p.Visible = false
            TabButtons[name].BackgroundColor3 = Color3.fromRGB(22, 23, 32)
            TabButtons[name].TextColor3 = Color3.fromRGB(160, 160, 175)
        end
        Pages[tabName].Visible = true
        TabBtn.BackgroundColor3 = CARD_COLOR
        TabBtn.TextColor3 = ACCENT_BLUE
    end)
end

---------------------------------------------------------
-- دالة إنشاء الأزرار العادية
---------------------------------------------------------
local function CreateToggleBtn(btnText, parentPage)
    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(1, -6, 0, 38)
    Btn.Text = btnText
    Btn.TextColor3 = TEXT_COLOR
    Btn.TextSize = 13
    Btn.Font = Enum.Font.GothamBold
    Btn.BackgroundColor3 = CARD_COLOR
    Btn.Parent = parentPage

    local BtnCorner = Instance.new("UICorner")
    BtnCorner.CornerRadius = UDim.new(0, 8)
    BtnCorner.Parent = Btn

    return Btn
end

---------------------------------------------------------
-- 1. تبويب اللاعب
---------------------------------------------------------
local PlayerPage = Pages["اللاعب"]
local PlayerLayout = Instance.new("UIListLayout")
PlayerLayout.SortOrder = Enum.SortOrder.LayoutOrder
PlayerLayout.Padding = UDim.new(0, 8)
PlayerLayout.Parent = PlayerPage

local function CreateInputRow(labelText, defaultVal, callback)
    local Row = Instance.new("Frame")
    Row.Size = UDim2.new(1, -6, 0, 42)
    Row.BackgroundColor3 = CARD_COLOR
    Row.Parent = PlayerPage

    local RowCorner = Instance.new("UICorner")
    RowCorner.CornerRadius = UDim.new(0, 8)
    RowCorner.Parent = Row

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(0, 110, 1, 0)
    Label.Position = UDim2.new(0, 12, 0, 0)
    Label.Text = labelText
    Label.TextColor3 = TEXT_COLOR
    Label.TextSize = 13
    Label.Font = Enum.Font.GothamBold
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.BackgroundTransparency = 1
    Label.Parent = Row

    local Box = Instance.new("TextBox")
    Box.Size = UDim2.new(0, 85, 0, 28)
    Box.Position = UDim2.new(0, 125, 0.5, -14)
    Box.Text = tostring(defaultVal)
    Box.TextColor3 = ACCENT_BLUE
    Box.TextSize = 13
    Box.Font = Enum.Font.GothamBold
    Box.BackgroundColor3 = Color3.fromRGB(16, 17, 24)
    Box.Parent = Row

    local BoxCorner = Instance.new("UICorner")
    BoxCorner.CornerRadius = UDim.new(0, 6)
    BoxCorner.Parent = Box

    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(0, 90, 0, 28)
    Btn.Position = UDim2.new(1, -98, 0.5, -14)
    Btn.Text = "تطبيق"
    Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    Btn.TextSize = 12
    Btn.Font = Enum.Font.GothamBold
    Btn.BackgroundColor3 = ACCENT_BLUE
    Btn.Parent = Row

    local BtnCorner = Instance.new("UICorner")
    BtnCorner.CornerRadius = UDim.new(0, 6)
    BtnCorner.Parent = Btn

    Btn.MouseButton1Click:Connect(function()
        local val = tonumber(Box.Text)
        if val then callback(val) end
    end)
end

CreateInputRow("السرعة:", 16, function(val)
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = val
    end
end)

CreateInputRow("قوة النط:", 50, function(val)
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.UseJumpPower = true
        LocalPlayer.Character.Humanoid.JumpPower = val
    end
end)

local FlyBtn = CreateToggleBtn("تشغيل Fly V3 🚀", PlayerPage)
FlyBtn.BackgroundColor3 = Color3.fromRGB(0, 140, 230)
FlyBtn.MouseButton1Click:Connect(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/XNEOFF/FlyGuiV3/main/FlyGuiV3.txt"))()
end)

---------------------------------------------------------
-- 2. تبويب الأنيميشنات
---------------------------------------------------------
local AnimPage = Pages["الأنيميشنات"]
local AnimScroll = Instance.new("ScrollingFrame")
AnimScroll.Size = UDim2.new(1, 0, 1, 0)
AnimScroll.BackgroundTransparency = 1
AnimScroll.ScrollBarThickness = 4
AnimScroll.CanvasSize = UDim2.new(0, 0, 0, 750)
AnimScroll.Parent = AnimPage

local AnimLayout = Instance.new("UIListLayout")
AnimLayout.SortOrder = Enum.SortOrder.LayoutOrder
AnimLayout.Padding = UDim.new(0, 8)
AnimLayout.Parent = AnimScroll

local activeTrack = nil

local function PlayAnimation(id)
    local char = LocalPlayer.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum then return end

    if activeTrack then
        activeTrack:Stop()
        activeTrack = nil
    end

    if id == 0 then return end

    local anim = Instance.new("Animation")
    anim.AnimationId = "rbxassetid://" .. tostring(id)
    activeTrack = hum:LoadAnimation(anim)
    activeTrack.Priority = Enum.AnimationPriority.Action
    activeTrack:Play()
end

local StopAnimBtn = Instance.new("TextButton")
StopAnimBtn.Size = UDim2.new(1, -6, 0, 38)
StopAnimBtn.Text = "🛑 إيقاف الأنيميشن"
StopAnimBtn.TextColor3 = Color3.fromRGB(255, 90, 90)
StopAnimBtn.TextSize = 13
StopAnimBtn.Font = Enum.Font.GothamBold
StopAnimBtn.BackgroundColor3 = Color3.fromRGB(40, 20, 25)
StopAnimBtn.Parent = AnimScroll

local StopCorner = Instance.new("UICorner")
StopCorner.CornerRadius = UDim.new(0, 8)
StopCorner.Parent = StopAnimBtn

StopAnimBtn.MouseButton1Click:Connect(function()
    PlayAnimation(0)
end)

local AnimationsList = {
    {Name = "1. 💥 Dab (داب)", ID = 248263260},
    {Name = "2. 🛐 Crouch (جثو / انحناء)", ID = 180436334},
    {Name = "3. 💃 Floss Dance (رقصة الفلوس)", ID = 10714013233},
    {Name = "4. 🏋️ Pushups (تمرين الضغط)", ID = 180436148},
    {Name = "5. 👏 Cheer (تشجيع)", ID = 129423030},
    {Name = "6. 👋 Wave (تلويح باليد)", ID = 128853357},
    {Name = "7. 🕺 Dance 1 (رقصة 1)", ID = 182435998},
    {Name = "8. 🕺 Dance 2 (رقصة 2)", ID = 182436842},
    {Name = "9. 🕺 Dance 3 (رقصة 3)", ID = 182436935},
    {Name = "10. 🤔 Point / Think (تفكير)", ID = 158821881},
    {Name = "11. 🥱 Yawn / Rest (استراحة)", ID = 259209880},
    {Name = "12. 🥋 Ninja Flex (استعراض القوة)", ID = 333333131},
    {Name = "13. 🤦 Facepalm (ضرب الوجه)", ID = 284882546},
    {Name = "14. 🧟 Zombie Walk (مشية الزومبي)", ID = 616158082},
    {Name = "15. 📐 Sit Floor (جلوس أرضي)", ID = 179224234}
}

for _, animData in ipairs(AnimationsList) do
    local Btn = CreateToggleBtn(animData.Name, AnimScroll)
    Btn.MouseButton1Click:Connect(function()
        PlayAnimation(animData.ID)
    end)
end

---------------------------------------------------------
-- 3. تبويب استهداف (Target System)
---------------------------------------------------------
local TargetPage = Pages["استهداف"]
local TargetPlayerSelected = nil
local CurrentHighlight = nil
local IsSpectating = false

-- 🔴 [المربع الأحمر]: صندوق كتابة والبحث عن الاسم
local SearchBox = Instance.new("TextBox")
SearchBox.Name = "SearchBox"
SearchBox.Size = UDim2.new(0.52, 0, 0, 36)
SearchBox.Position = UDim2.new(0.48, 0, 0, 0)
SearchBox.PlaceholderText = "🔍 اكتب اسم اللاعب..."
SearchBox.PlaceholderColor3 = Color3.fromRGB(130, 130, 145)
SearchBox.Text = ""
SearchBox.TextColor3 = TEXT_COLOR
SearchBox.TextSize = 12
SearchBox.Font = Enum.Font.GothamBold
SearchBox.BackgroundColor3 = CARD_COLOR
SearchBox.Parent = TargetPage

local SearchCorner = Instance.new("UICorner")
SearchCorner.CornerRadius = UDim.new(0, 8)
SearchCorner.Parent = SearchBox

-- 🟡 [المربع الأصفر]: قائمة طويلة بكل اللاعبين في السيرفر
local ServerPlayersList = Instance.new("ScrollingFrame")
ServerPlayersList.Name = "ServerPlayersList"
ServerPlayersList.Size = UDim2.new(0.52, 0, 1, -44)
ServerPlayersList.Position = UDim2.new(0.48, 0, 0, 44)
ServerPlayersList.BackgroundColor3 = Color3.fromRGB(20, 21, 30)
ServerPlayersList.ScrollBarThickness = 4
ServerPlayersList.ScrollBarImageColor3 = ACCENT_BLUE
ServerPlayersList.Parent = TargetPage

local ListCorner = Instance.new("UICorner")
ListCorner.CornerRadius = UDim.new(0, 8)
ListCorner.Parent = ServerPlayersList

local ListLayout = Instance.new("UIListLayout")
ListLayout.SortOrder = Enum.SortOrder.LayoutOrder
ListLayout.Padding = UDim.new(0, 4)
ListLayout.Parent = ServerPlayersList

-- 🟣 [المربع البنفسجي]: صورة اللاعب المستهدف (Avatar / Skin)
local AvatarFrame = Instance.new("Frame")
AvatarFrame.Name = "AvatarFrame"
AvatarFrame.Size = UDim2.new(0.44, 0, 0, 125)
AvatarFrame.Position = UDim2.new(0, 0, 0, 0)
AvatarFrame.BackgroundColor3 = CARD_COLOR
AvatarFrame.Parent = TargetPage

local AvatarCorner = Instance.new("UICorner")
AvatarCorner.CornerRadius = UDim.new(0, 8)
AvatarCorner.Parent = AvatarFrame

local AvatarImage = Instance.new("ImageLabel")
AvatarImage.Size = UDim2.new(0, 100, 0, 100)
AvatarImage.Position = UDim2.new(0.5, -50, 0.4, -50)
AvatarImage.BackgroundTransparency = 1
AvatarImage.Image = "rbxassetid://0"
AvatarImage.Parent = AvatarFrame

local TargetNameLabel = Instance.new("TextLabel")
TargetNameLabel.Size = UDim2.new(1, 0, 0, 20)
TargetNameLabel.Position = UDim2.new(0, 0, 1, -22)
TargetNameLabel.Text = "لم يتم اختيار لاعب"
TargetNameLabel.TextColor3 = Color3.fromRGB(170, 170, 185)
TargetNameLabel.TextSize = 11
TargetNameLabel.Font = Enum.Font.Gotham
TargetNameLabel.BackgroundTransparency = 1
TargetNameLabel.Parent = AvatarFrame

-- 🔵 [المستطيلات الزرقاء الـ 3]: الأزرار الثلاثة
local ButtonsContainer = Instance.new("Frame")
ButtonsContainer.Size = UDim2.new(0.44, 0, 1, -133)
ButtonsContainer.Position = UDim2.new(0, 0, 0, 133)
ButtonsContainer.BackgroundTransparency = 1
ButtonsContainer.Parent = TargetPage

local BtnLayout = Instance.new("UIListLayout")
BtnLayout.SortOrder = Enum.SortOrder.LayoutOrder
BtnLayout.Padding = UDim.new(0, 6)
BtnLayout.Parent = ButtonsContainer

-- 1. زر الانتقال
local TPBtn = Instance.new("TextButton")
TPBtn.Size = UDim2.new(1, 0, 0, 35)
TPBtn.Text = "📍 انتقال إلى المستهدف"
TPBtn.TextColor3 = TEXT_COLOR
TPBtn.TextSize = 12
TPBtn.Font = Enum.Font.GothamBold
TPBtn.BackgroundColor3 = CARD_COLOR
TPBtn.Parent = ButtonsContainer

local TPCorner = Instance.new("UICorner")
TPCorner.CornerRadius = UDim.new(0, 8)
TPCorner.Parent = TPBtn

-- 2. زر المشاهدة
local SpectateBtn = Instance.new("TextButton")
SpectateBtn.Size = UDim2.new(1, 0, 0, 35)
SpectateBtn.Text = "👁️ مشاهدة المستهدف"
SpectateBtn.TextColor3 = TEXT_COLOR
SpectateBtn.TextSize = 12
SpectateBtn.Font = Enum.Font.GothamBold
SpectateBtn.BackgroundColor3 = CARD_COLOR
SpectateBtn.Parent = ButtonsContainer

local SpecCorner = Instance.new("UICorner")
SpecCorner.CornerRadius = UDim.new(0, 8)
SpecCorner.Parent = SpectateBtn

-- 3. زر التحديد (أحمر)
local HighlightBtn = Instance.new("TextButton")
HighlightBtn.Size = UDim2.new(1, 0, 0, 35)
HighlightBtn.Text = "🔴 تحديد المستهدف (أحمر)"
HighlightBtn.TextColor3 = TEXT_COLOR
HighlightBtn.TextSize = 12
HighlightBtn.Font = Enum.Font.GothamBold
HighlightBtn.BackgroundColor3 = CARD_COLOR
HighlightBtn.Parent = ButtonsContainer

local HLCorner = Instance.new("UICorner")
HLCorner.CornerRadius = UDim.new(0, 8)
HLCorner.Parent = HighlightBtn

---------------------------------------------------------
-- وظائف الاستهداف مع حماية من الأخطاء
---------------------------------------------------------
local function SelectTarget(plr)
    TargetPlayerSelected = plr
    TargetNameLabel.Text = plr.DisplayName .. " (@" .. plr.Name .. ")"
    TargetNameLabel.TextColor3 = ACCENT_BLUE

    pcall(function()
        AvatarImage.Image = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. plr.UserId .. "&width=150&height=150&format=png"
    end)
end

local function RefreshServerList(filterText)
    for _, child in pairs(ServerPlayersList:GetChildren()) do
        if child:IsA("TextButton") then child:Destroy() end
    end

    local filter = filterText or ""
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer then
            if filter == "" or string.find(string.lower(plr.Name), string.lower(filter)) or string.find(string.lower(plr.DisplayName), string.lower(filter)) then
                local Btn = Instance.new("TextButton")
                Btn.Size = UDim2.new(1, -6, 0, 30)
                Btn.Text = "  " .. plr.DisplayName .. " (@" .. plr.Name .. ")"
                Btn.TextColor3 = TEXT_COLOR
                Btn.TextSize = 11
                Btn.Font = Enum.Font.Gotham
                Btn.TextXAlignment = Enum.TextXAlignment.Left
                Btn.BackgroundColor3 = CARD_COLOR
                Btn.Parent = ServerPlayersList

                local BtnCorner = Instance.new("UICorner")
                BtnCorner.CornerRadius = UDim.new(0, 6)
                BtnCorner.Parent = Btn

                Btn.MouseButton1Click:Connect(function()
                    SelectTarget(plr)
                end)
            end
        end
    end
end

SearchBox:GetPropertyChangedSignal("Text"):Connect(function()
    RefreshServerList(SearchBox.Text)
end)

Players.PlayerAdded:Connect(function() RefreshServerList(SearchBox.Text) end)
Players.PlayerRemoving:Connect(function() RefreshServerList(SearchBox.Text) end)
RefreshServerList("")

-- 1. انتقال
TPBtn.MouseButton1Click:Connect(function()
    if TargetPlayerSelected and TargetPlayerSelected.Character and TargetPlayerSelected.Character:FindFirstChild("HumanoidRootPart") then
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            LocalPlayer.Character.HumanoidRootPart.CFrame = TargetPlayerSelected.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3)
        end
    end
end)

-- 2. مشاهدة
SpectateBtn.MouseButton1Click:Connect(function()
    if IsSpectating then
        IsSpectating = false
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
            workspace.CurrentCamera.CameraSubject = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        end
        SpectateBtn.Text = "👁️ مشاهدة المستهدف"
        SpectateBtn.BackgroundColor3 = CARD_COLOR
    else
        if TargetPlayerSelected and TargetPlayerSelected.Character and TargetPlayerSelected.Character:FindFirstChildOfClass("Humanoid") then
            IsSpectating = true
            workspace.CurrentCamera.CameraSubject = TargetPlayerSelected.Character:FindFirstChildOfClass("Humanoid")
            SpectateBtn.Text = "🛑 
