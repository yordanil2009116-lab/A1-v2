-- ANXAM HUB v1.0 | Reef Military
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- تنظيف الواجهات القديمة لمنع التكرار
if PlayerGui:FindFirstChild("AnxamCustomUI") then
    PlayerGui.AnxamCustomUI:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AnxamCustomUI"
ScreenGui.Parent = PlayerGui
ScreenGui.ResetOnSpawn = false

-- الألوان الرئيسية
local BG_COLOR = Color3.fromRGB(15, 15, 22)
local CARD_COLOR = Color3.fromRGB(24, 25, 36)
local ACCENT_BLUE = Color3.fromRGB(0, 170, 255)
local TEXT_COLOR = Color3.fromRGB(240, 240, 245)

-- الإطار الرئيسي
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 560, 0, 330)
MainFrame.Position = UDim2.new(0.5, -280, 0.5, -165)
MainFrame.BackgroundColor3 = BG_COLOR
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = true
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = ACCENT_BLUE
MainStroke.Thickness = 1.5
MainStroke.Transparency = 0.4
MainStroke.Parent = MainFrame

-- الشريط العلوي
local TopBar = Instance.new("Frame")
TopBar.Size = UDim2.new(1, 0, 0, 42)
TopBar.BackgroundColor3 = Color3.fromRGB(20, 21, 30)
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -90, 1, 0)
Title.Position = UDim2.new(0, 15, 0, 0)
Title.Text = "⚡ ANXAM HUB v1.0 | Reef Military"
Title.TextColor3 = TEXT_COLOR
Title.TextSize = 15
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.BackgroundTransparency = 1
Title.Parent = TopBar

-- زر الإغلاق X
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 28, 0, 28)
CloseBtn.Position = UDim2.new(1, -34, 0, 7)
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(255, 90, 90)
CloseBtn.TextSize = 14
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.BackgroundColor3 = Color3.fromRGB(35, 30, 40)
CloseBtn.Parent = TopBar

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 8)
CloseCorner.Parent = CloseBtn

-- زر التصغير -
local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Size = UDim2.new(0, 28, 0, 28)
MinimizeBtn.Position = UDim2.new(1, -68, 0, 7)
MinimizeBtn.Text = "─"
MinimizeBtn.TextColor3 = TEXT_COLOR
MinimizeBtn.TextSize = 12
MinimizeBtn.Font = Enum.Font.GothamBold
MinimizeBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
MinimizeBtn.Parent = TopBar

local MiniBtnCorner = Instance.new("UICorner")
MiniBtnCorner.CornerRadius = UDim.new(0, 8)
MiniBtnCorner.Parent = MinimizeBtn

-- الزر الصغير العائم
local MiniToggle = Instance.new("TextButton")
MiniToggle.Name = "MiniToggle"
MiniToggle.Size = UDim2.new(0, 130, 0, 38)
MiniToggle.Position = UDim2.new(0.02, 0, 0.15, 0)
MiniToggle.Text = "⚡ ANXAM HUB"
MiniToggle.TextColor3 = ACCENT_BLUE
MiniToggle.TextSize = 13
MiniToggle.Font = Enum.Font.GothamBold
MiniToggle.BackgroundColor3 = BG_COLOR
MiniToggle.Active = true
MiniToggle.Draggable = true
MiniToggle.Visible = false
MiniToggle.Parent = ScreenGui

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(0, 10)
ToggleCorner.Parent = MiniToggle

MiniToggle.MouseButton1Click:Connect(function()
    MiniToggle.Visible = false
    MainFrame.Visible = true
end)

MinimizeBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    MiniToggle.Visible = true
end)

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- شريط التبويبات
local TabBar = Instance.new("Frame")
TabBar.Size = UDim2.new(0, 135, 1, -52)
TabBar.Position = UDim2.new(0, 8, 0, 46)
TabBar.BackgroundColor3 = Color3.fromRGB(18, 19, 26)
TabBar.Parent = MainFrame

local TabCorner = Instance.new("UICorner")
TabCorner.CornerRadius = UDim.new(0, 8)
TabCorner.Parent = TabBar

local TabLayout = Instance.new("UIListLayout")
TabLayout.SortOrder = Enum.SortOrder.LayoutOrder
TabLayout.Padding = UDim.new(0, 6)
TabLayout.Parent = TabBar

local Container = Instance.new("Frame")
Container.Size = UDim2.new(1, -157, 1, -52)
Container.Position = UDim2.new(0, 149, 0, 46)
Container.BackgroundColor3 = Color3.fromRGB(18, 19, 26)
Container.Parent = MainFrame

local ContainerCorner = Instance.new("UICorner")
ContainerCorner.CornerRadius = UDim.new(0, 8)
ContainerCorner.Parent = Container

local Tabs = {"اللاعب", "الأنيميشنات", "استهداف", "المواقع", "قائمة جديدة"}
local Pages = {}
local TabButtons = {}

for i, tabName in ipairs(Tabs) do
    local Page = Instance.new("ScrollingFrame")
    Page.Name = tabName .. "_Page"
    Page.Size = UDim2.new(1, -12, 1, -12)
    Page.Position = UDim2.new(0, 6, 0, 6)
    Page.BackgroundTransparency = 1
    Page.Visible = (i == 1)
    Page.ScrollBarThickness = 3
    Page.ScrollBarImageColor3 = ACCENT_BLUE
    Page.CanvasSize = UDim2.new(0, 0, 0, 500)
    Page.Parent = Container
    Pages[tabName] = Page

    local PageLayout = Instance.new("UIListLayout")
    PageLayout.SortOrder = Enum.SortOrder.LayoutOrder
    PageLayout.Padding = UDim.new(0, 8)
    PageLayout.Parent = Page

    local TabBtn = Instance.new("TextButton")
    TabBtn.Size = UDim2.new(1, 0, 0, 36)
    TabBtn.Text = tabName
    TabBtn.TextColor3 = (i == 1) and ACCENT_BLUE or Color3.fromRGB(160, 160, 175)
    TabBtn.TextSize = 13
    TabBtn.Font = Enum.Font.GothamBold
    TabBtn.BackgroundColor3 = (i == 1) and CARD_COLOR or Color3.fromRGB(22, 23, 32)
    TabBtn.Parent = TabBar

    local BtnCorner = Instance.new("UICorner")
    BtnCorner.CornerRadius = UDim.new(0, 6)
    BtnCorner.Parent = TabBtn

    TabButtons[tabName] = TabBtn

    TabBtn.MouseButton1Click:Connect(function()
        for name, p in pairs(Pages) do
            p.Visible = false
            TabButtons[name].BackgroundColor3 = Color3.fromRGB(22, 23, 32)
            TabButtons[name].TextColor3 = Color3.fromRGB(160, 160, 175)
        end
        Pages[tabName].Visible = true
        TabBtn.BackgroundColor3 = CARD_COLOR
        TabBtn.TextColor3 = ACCENT_BLUE
    end)
end

---------------------------------------------------------
-- محتويات تبويب "اللاعب"
---------------------------------------------------------
local PlayerPage = Pages["اللاعب"]

local function CreateInputRow(labelText, defaultVal)
    local Row = Instance.new("Frame")
    Row.Size = UDim2.new(1, -6, 0, 42)
    Row.BackgroundColor3 = CARD_COLOR
    Row.Parent = PlayerPage

    local RowCorner = Instance.new("UICorner")
    RowCorner.CornerRadius = UDim.new(0, 8)
    RowCorner.Parent = Row

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(0, 110, 1, 0)
    Label.Position = UDim2.new(0, 12, 0, 0)
    Label.Text = labelText
    Label.TextColor3 = TEXT_COLOR
    Label.TextSize = 13
    Label.Font = Enum.Font.GothamBold
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.BackgroundTransparency = 1
    Label.Parent = Row

    local Box = Instance.new("TextBox")
    Box.Size = UDim2.new(0, 85, 0, 28)
    Box.Position = UDim2.new(0, 125, 0.5, -14)
    Box.Text = tostring(defaultVal)
    Box.TextColor3 = ACCENT_BLUE
    Box.TextSize = 13
    Box.Font = Enum.Font.GothamBold
    Box.BackgroundColor3 = Color3.fromRGB(16, 17, 24)
    Box.Parent = Row

    local BoxCorner = Instance.new("UICorner")
    BoxCorner.CornerRadius = UDim.new(0, 6)
    BoxCorner.Parent = Box

    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(0, 100, 0, 28)
    Btn.Position = UDim2.new(1, -108, 0.5, -14)
    Btn.Text = "تطبيق"
    Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    Btn.TextSize = 12
    Btn.Font = Enum.Font.GothamBold
    Btn.BackgroundColor3 = ACCENT_BLUE
    Btn.Parent = Row

    local BtnCorner = Instance.new("UICorner")
    BtnCorner.CornerRadius = UDim.new(0, 6)
    BtnCorner.Parent = Btn

    return Box, Btn
end

local function CreateToggleBtn(btnText, parentPage)
    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(1, -6, 0, 38)
    Btn.Text = btnText
    Btn.TextColor3 = TEXT_COLOR
    Btn.TextSize = 13
    Btn.Font = Enum.Font.GothamBold
    Btn.BackgroundColor3 = CARD_COLOR
    Btn.Parent = parentPage or PlayerPage

    local BtnCorner = Instance.new("UICorner")
    BtnCorner.CornerRadius = UDim.new(0, 8)
    BtnCorner.Parent = Btn

    return Btn
end

-- 1. السرعة
local SpeedBox, SpeedBtn = CreateInputRow("السرعة:", 16)
SpeedBtn.MouseButton1Click:Connect(function()
    local val = tonumber(SpeedBox.Text)
    if val and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = val
    end
end)

-- 2. قوة القفز
local JumpBox, JumpBtn = CreateInputRow("قوة النط:", 50)
JumpBtn.MouseButton1Click:Connect(function()
    local val = tonumber(JumpBox.Text)
    if val and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.UseJumpPower = true
        LocalPlayer.Character.Humanoid.JumpPower = val
    end
end)

-- 3. مدى الرؤية
local FovBox, FovBtn = CreateInputRow("مدى الرؤية:", 70)
FovBtn.MouseButton1Click:Connect(function()
    local val = tonumber(FovBox.Text)
    if val then
        workspace.CurrentCamera.FieldOfView = val
    end
end)

-- 4. Fly V3
local FlyV3Btn = CreateToggleBtn("تشغيل Fly V3 🚀", PlayerPage)
FlyV3Btn.BackgroundColor3 = Color3.fromRGB(0, 140, 230)
FlyV3Btn.MouseButton1Click:Connect(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/XNEOFF/FlyGuiV3/main/FlyGuiV3.txt"))()
end)

-- 5. الرؤية الليلية
local fullbright = false
local FullbrightBtn = CreateToggleBtn("الرؤية الليلية: معطل", PlayerPage)
FullbrightBtn.MouseButton1Click:Connect(function()
    fullbright = not fullbright
    FullbrightBtn.Text = "الرؤية الليلية: " .. (fullbright and "مفعل 🟢" or "معطل 🔴")
    FullbrightBtn.TextColor3 = fullbright and Color3.fromRGB(0, 230, 130) or TEXT_COLOR
    
    if fullbright then
        Lighting.Ambient = Color3.fromRGB(255, 255, 255)
        Lighting.Brightness = 2
        Lighting.ClockTime = 14
    else
        Lighting.Ambient = Color3.fromRGB(128, 128, 128)
        Lighting.Brightness = 1
    end
end)

-- 6. تخطي الجدران
local noclip = false
local NoclipBtn = CreateToggleBtn("تخطي الجدران: معطل", PlayerPage)
NoclipBtn.MouseButton1Click:Connect(function()
    noclip = not noclip
    NoclipBtn.Text = "تخطي الجدران: " .. (noclip and "مفعل 🟢" or "معطل 🔴")
    NoclipBtn.TextColor3 = noclip and Color3.fromRGB(0, 230, 130) or TEXT_COLOR
end)

RunService.Stepped:Connect(function()
    if noclip and LocalPlayer.Character then
        for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end
end)

-- 7. قفز لانهائي
local infJump = false
local InfJumpBtn = CreateToggleBtn("قفز لانهائي: معطل", PlayerPage)
InfJumpBtn.MouseButton1Click:Connect(function()
    infJump = not infJump
    InfJumpBtn.Text = "قفز لانهائي: " .. (infJump and "مفعل 🟢" or "معطل 🔴")
    InfJumpBtn.TextColor3 = infJump and Color3.fromRGB(0, 230, 130) or TEXT_COLOR
end)

UserInputService.JumpRequest:Connect(function()
    if infJump and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)

---------------------------------------------------------
-- محتويات تبويب "الأنيميشنات" (تشغيل قائمة R6 Animation الكاملة)
---------------------------------------------------------
local AnimPage = Pages["الأنيميشنات"]

local LoadR6AnimBtn = Instance.new("TextButton")
LoadR6AnimBtn.Size = UDim2.new(1, -6, 0, 48)
LoadR6AnimBtn.Text = "💃 فتح قائمة أنيميشنات R6 الكاملة"
LoadR6AnimBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
LoadR6AnimBtn.TextSize = 14
LoadR6AnimBtn.Font = Enum.Font.GothamBold
LoadR6AnimBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
LoadR6AnimBtn.Parent = AnimPage

local LoadR6Corner = Instance.new("UICorner")
LoadR6Corner.CornerRadius = UDim.new(0, 8)
LoadR6Corner.Parent = LoadR6AnimBtn

LoadR6AnimBtn.MouseButton1Click:Connect(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/جلب-سكربت-R6-الانيميشن/R6Animations.lua", true))()
end)

local AnimInfoLabel = Instance.new("TextLabel")
AnimInfoLabel.Size = UDim2.new(1, -6, 0, 60)
AnimInfoLabel.Text = "عند الضغط على الزر بالأعلى، ستفتح نافذة R6 Animation السوداء الموضحة في الصورة وتحتوي على جميع الأنيميشنات والرقصات والبحث."
AnimInfoLabel.TextColor3 = Color3.fromRGB(160, 160, 175)
AnimInfoLabel.TextSize = 12
AnimInfoLabel.Font = Enum.Font.Gotham
AnimInfoLabel.TextWrapped = true
AnimInfoLabel.BackgroundTransparency = 1
AnimInfoLabel.Parent = AnimPage
---------------------------------------------------------
-- محتويات تبويب "استهداف" (تتبع وانتقال للاعبين)
---------------------------------------------------------
local TargetPage = Pages["استهداف"]

local SelectedPlayer = nil

local TargetDropdown = Instance.new("TextButton")
TargetDropdown.Size = UDim2.new(1, -6, 0, 36)
TargetDropdown.Text = "اختر لاعباً من القائمة... 🔻"
TargetDropdown.TextColor3 = TEXT_COLOR
TargetDropdown.TextSize = 13
TargetDropdown.Font = Enum.Font.GothamBold
TargetDropdown.BackgroundColor3 = CARD_COLOR
TargetDropdown.Parent = TargetPage

local DropCorner = Instance.new("UICorner")
DropCorner.CornerRadius = UDim.new(0, 8)
DropCorner.Parent = TargetDropdown

local DropList = Instance.new("ScrollingFrame")
DropList.Size = UDim2.new(1, -6, 0, 120)
DropList.BackgroundColor3 = Color3.fromRGB(20, 21, 30)
DropList.Visible = false
DropList.ScrollBarThickness = 3
DropList.Parent = TargetPage

local DropLayout = Instance.new("UIListLayout")
DropLayout.SortOrder = Enum.SortOrder.LayoutOrder
DropLayout.Padding = UDim.new(0, 4)
DropLayout.Parent = DropList

local function UpdatePlayerList()
    for _, child in pairs(DropList:GetChildren()) do
        if child:IsA("TextButton") then child:Destroy() end
    end
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer then
            local Btn = Instance.new("TextButton")
            Btn.Size = UDim2.new(1, 0, 0, 28)
            Btn.Text = plr.DisplayName .. " (@" .. plr.Name .. ")"
            Btn.TextColor3 = TEXT_COLOR
            Btn.TextSize = 12
            Btn.Font = Enum.Font.Gotham
            Btn.BackgroundColor3 = CARD_COLOR
            Btn.Parent = DropList

            Btn.MouseButton1Click:Connect(function()
                SelectedPlayer = plr
                TargetDropdown.Text = "المستهدف: " .. plr.DisplayName
                DropList.Visible = false
            end)
        end
    end
end

TargetDropdown.MouseButton1Click:Connect(function()
    DropList.Visible = not DropList.Visible
    if DropList.Visible then UpdatePlayerList() end
end)

-- زر الانتقال التلقائي لللاعب
local TeleportToBtn = CreateToggleBtn("انتقال إلى اللاعب المستهدف 📍", TargetPage)
TeleportToBtn.MouseButton1Click:Connect(function()
    if SelectedPlayer and SelectedPlayer.Character and SelectedPlayer.Character:FindFirstChild("HumanoidRootPart") then
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            LocalPlayer.Character.HumanoidRootPart.CFrame = SelectedPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3)
        end
    end
end)

-- زر مشاهدة اللاعب (Spectate)
local spectating = false
local SpectateBtn = CreateToggleBtn("مشاهدة اللاعب: معطل 👁️", TargetPage)
SpectateBtn.MouseButton1Click:Connect(function()
    spectating = not spectating
    SpectateBtn.Text = "مشاهدة اللاعب: " .. (spectating and "مفعل 🟢" or "معطل 🔴")
    SpectateBtn.TextColor3 = spectating and Color3.fromRGB(0, 230, 130) or TEXT_COLOR
    
    local Cam = workspace.CurrentCamera
    if spectating and SelectedPlayer and SelectedPlayer.Character and SelectedPlayer.Character:FindFirstChild("Humanoid") then
        Cam.CameraSubject = SelectedPlayer.Character.Humanoid
    else
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
            Cam.CameraSubject = LocalPlayer.Character.Humanoid
        end
    end
end)

---------------------------------------------------------
-- محتويات تبويب "المواقع" (مواقع Barem Reef Military)
---------------------------------------------------------
local LocPage = Pages["المواقع"]

local Locations = {
    {Name = "المقر الرئيسي (HQ)", Pos = Vector3.new(0, 50, 0)},
    {Name = "منطقة الرصيف (Docks)", Pos = Vector3.new(150, 20, -200)},
    {Name = "مهبط الهليكوبتر (Helipad)", Pos = Vector3.new(-100, 35, 100)},
    {Name = "بوابة التفتيش (Gate)", Pos = Vector3.new(50, 15, -100)}
}

for _, loc in ipairs(Locations) do
    local LocBtn = CreateToggleBtn("📌 " .. loc.Name, LocPage)
    LocBtn.MouseButton1Click:Connect(function()
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(loc.Pos)
        end
    end)
end

---------------------------------------------------------
-- محتويات تبويب "قائمة جديدة" (مساحة إضافية)
---------------------------------------------------------
local CustomPage = Pages["قائمة جديدة"]

local CustomInfo = Instance.new("TextLabel")
CustomInfo.Size = UDim2.new(1, -6, 0, 50)
CustomInfo.Text = "يمكنك إضافة اختصارات أو أزرار إضافية هنا مستقبلاً."
CustomInfo.TextColor3 = Color3.fromRGB(160, 160, 175)
CustomInfo.TextSize = 13
CustomInfo.Font = Enum.Font.Gotham
CustomInfo.BackgroundTransparency = 1
CustomInfo.Parent = CustomPage
